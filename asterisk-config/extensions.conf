[internal]
exten => 6001,1,Answer()
same => n,Set(CHANNEL(musicclass)=default)
same => n,Wait(1)
same => n,Gosub(ai-conversation,start,1)
same => n,Hangup()

[ai-conversation]
exten => start,1,NoOp(Starting AI conversation)
same => n,AGI(agi://host.docker.internal:8000/api/calls/asterisk-agi)
same => n,Return
